apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "desafio-aula04.fullname" . }}-test-connection"
  labels:
    {{- include "desafio-aula04.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "desafio-aula04.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
