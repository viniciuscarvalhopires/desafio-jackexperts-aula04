apiVersion: v1
kind: Service
metadata:
  name: {{ include "desafio-aula04.fullname" . }}
  labels:
    {{- include "desafio-aula04.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "desafio-aula04.selectorLabels" . | nindent 4 }}
